# Optional: override the backend API base URL used by the React frontend.
# Copy this file to frontend/.env.local only when your backend is not running on http://localhost:5000.
# Example (PowerShell): Copy-Item .\frontend\.env.local.example .\frontend\.env.local
REACT_APP_API_URL=http://localhost:5000
